[build]
  # Directory to publish (the built output)
  publish = "dist"
  # Build command
  command = "npm run build"

# Handle SPA routing - redirect all requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
